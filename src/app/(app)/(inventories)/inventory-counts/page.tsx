import React from "react";
import Recipes from "./InventoryCounts";

export const metadata = {
  title: "Wobbly Pan | Recipes",
  description: "Recipes page with React Hook Form and React Query",
};

export default function page() {
  return <Recipes />;
}
