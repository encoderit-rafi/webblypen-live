import React from "react";
import Expenses from "./Expenses";

export const metadata = {
  title: "Wobbly Pan | Expenses",
  description: "Expenses page with React Hook Form and React Query",
};

export default function page() {
  return <Expenses />;
}
