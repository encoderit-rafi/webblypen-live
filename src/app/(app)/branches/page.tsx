import Branches from "./Branches";
export const metadata = {
  title: "Wobbly Pan | Branches",
  description: "Branches page with React Hook Form and React Query",
};

export default function page() {
  return <Branches />;
}
