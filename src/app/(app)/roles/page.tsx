import React from "react";
import Roles from "./Roles";

export const metadata = {
  title: "Wobbly Pan | Roles",
  description: "Roles page with React Hook Form and React Query",
};

export default function page() {
  return <Roles />;
}
