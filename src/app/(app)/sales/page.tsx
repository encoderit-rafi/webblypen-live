import React from "react";
import Sales from "./Sales";

export const metadata = {
  title: "Wobbly Pan | Sales",
  description: "Sales page with React Hook Form and React Query",
};

export default function page() {
  return (
    <div>
      <Sales />
    </div>
  );
}
