import React from "react";
import Units from "./Units";

export const metadata = {
  title: "Wobbly Pan | Units",
  description: "Units page with React Hook Form and React Query",
};

export default function page() {
  return <Units />;
}
